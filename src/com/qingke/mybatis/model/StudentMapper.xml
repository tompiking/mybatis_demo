<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qingke.mybatis.model.StudentMapper">
    <select id="selectOneStudent" resultType="com.qingke.mybatis.model.Student" parameterType="int">
    	select * from student where id=#{id}
    </select>
</mapper>